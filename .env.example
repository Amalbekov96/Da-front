VITE_API_BASE_URL=http://localhost:8000

# Google OAuth2 Client ID — same value as GOOGLE_CLIENT_ID in the backend's .env.
# Create one at https://console.cloud.google.com/apis/credentials (Web application,
# add http://localhost:5173 under Authorized JavaScript origins).
VITE_GOOGLE_CLIENT_ID=
